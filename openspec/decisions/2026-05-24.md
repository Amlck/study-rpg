# Decisions — 2026-05-24

## 15:00 — Leaderboard tier display bug — Option B 已決定、下個 session 開 propose 實作

問題：`https://med-study-rpg.com/2nd/#/leaderboard` 等級欄顯示 `T1/T2/T3`、沒對齊 `add-abbreviated-tier-labels-medexam2` (2026-05-23 archive) 規範的「診所/區域/醫中/大廟」短稱。**T4 玩家完全消失 / 跟 T3 混在一起**。

**Diagnosis（兩層 bug）**：

1. **Display layer** — `apps/medexam2-hospital-tw/src/pages/LeaderboardPage.tsx:364+494` 直接 render `T${row.hospital_tier}` 整數、沒呼叫 `tierLabel()`。
2. **Schema layer** — D1 `leaderboard_m2` `CHECK (hospital_tier BETWEEN 1 AND 3)` 只接 1-3；`apps/medexam2-hospital-tw/src/lib/sync/leaderboard.ts` `TIER_TO_NUMBER` 把 `國家級教學醫院` clamp 到 3、跟 `醫學中心` 撞號。結果：所有 T4 玩家 leaderboard 上等於 T3、無法區分。註解早標 "Phase 4 follow-up: bump Worker TIER_MAX to 4"、一直沒做。

**附加觀察**：owner 帳號（user `tony85314@gmail.com`，#9 康勞德）實際是大廟 T4 但 leaderboard 顯示 T1，可能是早期 push 時 stale row 從沒更新到 T3+（cross-check 下次：`wrangler d1 execute study-rpg-leaderboard --remote --command "SELECT user_id, hospital_tier, updated_at FROM leaderboard_m2 WHERE nickname LIKE '%康勞德%'"`）。

**Option B 決定**（完整修，Wave A frontend + Wave B schema）：

- **新 change folder**: `openspec/changes/fix-leaderboard-tier-display-and-t4/`
- **Wave A — display fix**（純 frontend、立即可 ship）：
  - `LeaderboardPage.tsx` 兩處 `T${n}` 換成 `tierLabel(NUM_TO_TIER[n])`、import 新 `NUM_TO_TIER` map (`1→診所`, `2→區域醫院`, `3→醫學中心`, `4→國家級教學醫院`)
  - 短稱 via 既有 `lib/tier-labels.ts` `tierLabel()` (→「診所/區域/醫中/大廟」)
  - Column header 保留「等級」
- **Wave B — T4 distinction**（需 D1 migration、owner manual apply）：
  - 新 D1 migration `0004_bump_tier_to_4.sql`（0003 已由 add-hospital-leaderboard-correct-count-filter propose 預留、按順序往後）：
    - SQLite 不支援 ALTER TABLE DROP CHECK constraint → 用 `CREATE TABLE leaderboard_m2_new AS SELECT * FROM leaderboard_m2` + new schema + DROP + RENAME pattern
    - 或者用 SQLite 12+ 的 `ALTER TABLE ... DROP CONSTRAINT` 如果 D1 支援（要 verify）
  - `cloudflare/sync-worker/src/leaderboard.ts` `TIER_MAX` 從 3 → 4、sanity bound check 同步
  - `apps/medexam2-hospital-tw/src/lib/sync/leaderboard.ts` `TIER_TO_NUMBER.國家級教學醫院: 4` 不再 clamp、移除舊 comment
  - Spec delta `hospital-leaderboard`: MODIFIED `hospital_tier ∈ [1, 4]` (was [1, 3])
- **Apply 順序**: A 可獨立 ship（先讓 T1-T3 顯示正確短稱、T4 玩家暫時仍混在 T3）；B 之後 ship（owner manual `wrangler d1 migrations apply --remote`、existing 3 rows 不動但 next push 會自動更新到 4）
- **跟其他 in-flight change 不撞**：
  - `add-hospital-leaderboard-correct-count-filter` propose 預留 0003、本 change 0004 接續、不撞
  - `add-hospital-equipment-medexam2` 已 ship、跟 leaderboard fix 沒交集
- **Migration 編號順序最終**: 0001 (existing) → 0002_add_badges (achievement, shipped) → 0003_add_total_correct (leaderboard-correct propose) → 0004_bump_tier_to_4 (本 change)

**下個 session 怎麼開工**：

1. `/opsx:propose fix-leaderboard-tier-display-and-t4`（context 重建後自然走 OpenSpec workflow，本 decision 提供 design draft）
2. Wave A: edit LeaderboardPage.tsx + add NUM_TO_TIER map + ship via commit + Chrome MCP verify
3. Wave B: write 0004 SQL + Worker code edit + client TIER_TO_NUMBER unclamp + spec delta + owner manual `wrangler d1 migrations apply --remote`
4. Spec delta on `hospital-leaderboard` 兩處 modified requirement: `hospital_tier ∈ [1, 4]` 範圍 + display 用 short label 透過 tierLabel()

**Source pointers for next session 不用重 grep**：
- Bug locations: `LeaderboardPage.tsx:364` + `:494`
- TIER_TO_NUMBER clamp: `lib/sync/leaderboard.ts` (search for `國家級教學醫院: 3` + the "// We clamp tier 4 → 3" comment)
- D1 schema: `cloudflare/sync-worker/migrations/0001_leaderboard.sql` line containing `CHECK (hospital_tier BETWEEN 1 AND 3)`
- tierLabel(): `apps/medexam2-hospital-tw/src/lib/tier-labels.ts` 已存在、不用新寫

## 13:00 — GH Actions storage 警示 root cause = 二階題庫 PNG（治本要等 R2 archive 後另開 change）

5/23 早上的「You have used 100% of the Actions storage included」警示，當天做了**治標**處理：
- 砍 28 個舊 `github-pages` artifact（3836 MB → 288 MB）
- 兩個 deploy workflow 加 `cancel-in-progress: true`（避免連續 push 累積）

今天 audit `add-r2-cloud-sync-migration` task §9.1 才發現**真正的 root cause** 是別的：

> 「`apps/medexam2-hospital-tw/dist/images/medexam2-tw/` ships 396 question PNGs (X-ray / CT / MRI / pathology) totalling **77 MB**. Pages artifact rebuilds + retains **90 MB per deploy** → 5 deploys/day burst saturates the 500 MB free-tier quota.」

也就是說 144 MB pages artifact 裡 **77 MB 是 二階題庫的醫學影像 PNG**。即使加了 cancel-in-progress，每天 1–2 個成功 deploy 仍會 retain ~144 MB；連 4 天就到 0.5 GB cap。

**治本路徑**（R2 migration 自己標為 out-of-scope follow-up）：
1. 等 `add-r2-cloud-sync-migration` archive（estimated ~2026-06-12 Phase 4 + 14-day bake）
2. 開新 change `host-medexam2-images-on-r2`：
   - 把 396 PNG 移到 R2 public bucket（`study-rpg-images` 或類似名）
   - 加 build-time env var `VITE_IMAGES_BASE_URL`
   - 圖檔 reference 改成 `${VITE_IMAGES_BASE_URL}/images/medexam2-tw/<id>.png`
   - 二階 build 不再帶 images → artifact ~99 MB → ~23 MB（-77%）
   - R2 zero egress 永遠免費

**短期措施夠不夠（5–6 月）**：今天做的治標措施 + 6/1 quota reset = 撐得過 R2 archive 之前的 ~3 週。若中途又爆，可以提早開 `host-medexam2-images-on-r2`（不需要等 R2 migration archive，純粹是 OpenSpec 紀律建議照順序）。

**順帶提**：醫療影像 PNG 移走後可順手做 WebP 轉檔再省 -40%（共 -86% from baseline），但這是優化非必要。

**追蹤**：在 R2 migration archive 後立刻開 change，避免下次 storage 警示重新從零 diagnosis。對應 R2 migration 的 task §9.1 已記錄這條 follow-up（標 out-of-scope）。

## 16:30 — supabase/functions/bug-triage-digest/ scaffold 保留現狀（不入 git、不刪）

5/22 寫的 Deno Supabase Edge Function（1148 bytes）— 拉最近 24hr 的 `bug_reports` 並輸出 JSON digest，靠 `x-triage-token` header 驗證。對應 M4.5 CLAUDE.md 提到的「future `/bug-reports` skill after the follow-up change」。

**為什麼留 untracked**：

- 是 real working code、不是 codex scratch（後者已加 `output/` 進 `.gitignore`）
- 但 2 天沒動、沒 deploy（無 GH Actions workflow、無 `supabase functions deploy bug-triage-digest` 紀錄、沒 `TRIAGE_TOKEN` secret 設定）
- 未來真要 deploy 時程式可能還會改（schedule 觸發 vs HTTP pull、digest 格式、Slack/email 通知 channel 都還沒定）
- 現在跑 `/opsx:propose add-bug-triage-edge-function` 太早，scope 還沒清楚

**未來啟動條件**：當 owner 真的要開 `/bug-reports` skill / 自動化 daily bug digest 時：

1. `/opsx:propose add-bug-triage-edge-function`
2. spec capability（規範 endpoint / token rotation / digest schema / schedule）
3. 加 `supabase/.gitignore` 例外 / 確認 `supabase/functions/` 該追蹤
4. 加 GH Actions workflow 跑 `supabase functions deploy`
5. 設 `TRIAGE_TOKEN` 進 Supabase secret store + 對應 GH Actions secret

**抗 stale 機制**：本 decision entry 是給未來 session 看的 — 下次 `/spec resume` load 最新 3 個 decisions 時會看到，不需要重新 discover。若 2026-06-30 前都沒動工，可考慮直接刪檔（abandoned）+ 補一筆 decision entry 記錄 abandonment reason。

## 17:00 — hotfix `a9006e4` (harden-backfill-chain-try-catch) Option A 直接驗證成立

`2026-05-24-leaderboard-badges-not-backfilling-other-players-handoff.md` 的 hypothesis 命中：`checkAssignmentInvariants()` 在原本 try/catch 之外，對部分玩家的 state throw 後 cascade 出 `onPullComplete`、把後續 `backfillMonotonicCounters()` + `backfillAchievementsFromCurrentStats()` 全部吞掉。Hotfix `a9006e4` 把 invariant 拉進同一 try/catch + 加 success log。

**驗證 timeline（皆 UTC）**：

| 事件 | 時間 | Δ from hotfix |
|---|---|---|
| Hotfix `a9006e4` commit + push | 08:45:35 | — |
| `康勞德測試` (b09401048 Google 帳號) 簽入並 push leaderboard | 08:47:52 | +137s |
| → D1 row `badges_csv = "quiz:P4,recruit:P4"` 寫入 ✅ | 同 | — |

CF Pages auto-deploy 大約 + 90-120s，跟 137s 的 delta 完全對齊 — 玩家拿到新 bundle 後 backfill 完整跑完。

**殘餘狀態**：8 個 hotfix-deploy 前簽入的玩家（LLL / ㄚㄚㄚ / kkk / Nigga / Amlck / 米漢堡大人 / hhh / van — van 是個例外其實已有 badges，可能是更早就在 fix 前作用就 work 那批）badges_csv 仍空。**Decision: 等玩家自然重新簽入**（下次開 app trigger fresh pull cycle + 新 client code → backfill 跑完寫 D1）。總共 < 10 玩家、最終一致性以週為單位即可。

**不啟動 Option C（server-side backfill）的理由**：
- 玩家規模小（< 10 row 殘餘）、不值得 60-90 min 寫 admin endpoint + service-role auth 鋪設
- 自然回流途徑明確（任一玩家下次開 app 就修好）
- 沒緊迫性（leaderboard 是 nice-to-have 視覺、玩家不會因為自己 badges 沒上而流失）

**未來訊號需要升級到 Option C**：若 2 週後仍 ≥ 5 row 殘餘空 badges_csv（代表自然回流路徑沒走通、或新 player 又持續用舊 client cache），再考慮 server-side。

## 17:30 — Option C 升級到 server-side backfill 後又延伸到 Worker ratchet + tooltip 改造

17:00 entry 寫的「等玩家自然回流」假設 player 會自然 close tab + reopen。User 隔天 push back：「但預設玩家不會重新登入，維持登入狀態的話有什麼解法？」追問後分析：active player tab 不關、保持 OAuth session、`onPullComplete` 雖然會在 visibilitychange fire 但跑的是 stale JS bundle 的 broken cascade、empty badges_csv 持續被 push。光等不會修。

選 Option A1（local script）一次性 backfill：寫 `scripts/backfill-leaderboard-badges.ts`（commit `3d49b34`）讀 D1 empty rows + R2 m2 bundle、跑 client backfill 同樣的 derivation logic、UPDATE D1。7 個 row dry-run 全部 derive 出非空 badges、apply 後 D1 10/10 都有 badges。

**但 LWW collision 立刻浮現**：apply 後 ~3 分鐘，kkk push 進一次 empty payload (updated_at = 09:13:19 UTC > my backfill 09:10:18)、Worker LWW 接受、把 badges_csv 蓋回空。

→ 開 hotfix `1cc4314` (one-way ratchet on badges_csv + subject_mastery_count)：在 `handleUpsert` UPSERT 加 `CASE WHEN excluded.X = '' AND leaderboard_m2.X != '' THEN leaderboard_m2.X ELSE excluded.X END`。意義：**empty incoming 不是 authoritative，而是 stale-client 的 artifact，treat as "no information" 而非 "negative information"**。Plain LWW 對 server-derived field 是錯的。

**Empirical 驗證 ratchet works** (17:46 Taiwan)：
- 17:46:05 backfill 第二次 UPDATE 進 D1: kkk badges = `quiz:P4,recruit:P3`
- 17:46:33 (28 秒後): D1 仍是 `quiz:P4,recruit:P3` 但 `updated_at = 17:46:33`
- updated_at 動了 → 期間有 kkk client push 進來，empty payload 沒蓋掉 → ratchet 確實在運作

順帶做 tooltip 改造 (`849e1dd`)：原本 hover badge 顯示「P4 級答題成就」之類的 category+tier label；改成從 ACHIEVEMENTS catalog lookup 第一個 (category, tier) match 的 achievement name（例：康勞德 quiz:P4 → 「初試啼聲」、recruit:P4 → 「首位醫師」）。catalog 順序慣例把 entry-threshold achievement 排第一、所以選 first match 對絕大多數玩家是「他們最可能 unlock 的那個」。Trade-off 文件記在 `BadgeSprite.tsx` inline comment：badges_csv 只 encode per-category highest tier、specific achievement(s) 未傳輸、選 first 是 best-guess approximation。

**Why：跨 session paper trail 給未來 session 看 — ratchet semantic 不直觀，下次有人看到 "為什麼空值不會蓋掉非空值" 會懵；tooltip approximation 限制也容易踩。寫進 decision entry 避免 rediscover。**

**狀態**：Worker ratchet + tooltip 都已 live on prod。kkk D1 已修但 KV cache 仍 stale；下一輪 hourly cron（18:00 Taiwan）propagate 後 leaderboard 頁面所有 10 row 應該都有 badges + 正確 hover 名稱。`add-version-check-banner` change (propose `94cbbea`) 仍未 apply、未來 deploy 涉及 client logic 改動之前先 apply 可徹底繞過 stale-cache 問題。
